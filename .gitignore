# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/

# Virtual environments
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Editors
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# chorus runtime state — the ledger, the board, and the memory/org git repos
# are local now-state, never committed (spec 00 §2, spec 01).
*.db
*.db-wal
*.db-shm
.chorus/
events*.jsonl
events*.jsonl.gz

# Local secrets — never commit
.env
.env.*
!.env.example

# uv
uv.lock
standup-app/hard-task-report/runs/*/tmp/
standup-app/hard-task-report/runs/*/run.log
reports/ceo-live-baseline.log

# M8 live-run transcripts — regenerated per qualification run; keep as external artifacts, not blobs
reports/M8-live-qualification.html
reports/t1-live-runs/
reports/t2-live-runs/
reports/t3-live-runs/
