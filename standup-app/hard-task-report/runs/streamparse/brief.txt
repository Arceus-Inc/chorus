Build an npm package `streamparse` that folds a raw SSE/token `ReadableStream` into a typed `AsyncIterable<StreamEvent>`: text deltas, tool-call deltas (assembled across chunks), usage accounting, and a terminal event. Ship a real `package.json` + `tsconfig.json`, build with `tsc` to `dist/`, and export the public types. `vitest` (or `node:test`) feeds canned chunk sequences and asserts the reconstructed events.
