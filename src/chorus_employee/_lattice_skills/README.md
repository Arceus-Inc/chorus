# lattice skills

Pattern consolidation skills for chorus employees (patterns-only branch scope).

| Skill | Tool(s) | When |
|---|---|---|
| `lattice-context` | `lattice_context`, `get_run` | Beat-start — patterns; `get_run(run_id)` on `src:` ids |
| `lattice-consolidate` | `lattice_packet`, `recall`, `get_run`, `lattice_apply` | Beat-end when gate open — recall slim hits, `get_run` full prose |

Authoritative copy lives in chorus (`chorus_employee/_lattice_skills/`). The lattice repo keeps directives and runtime only.
